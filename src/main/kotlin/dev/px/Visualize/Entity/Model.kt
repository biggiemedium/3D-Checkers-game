package dev.px.Visualize.Entity

class Model {

    var id: Int
    var vertexCount: Int

    constructor(id: Int, vertexCount: Int) {
        this.id = id
        this.vertexCount = vertexCount
    }

}